# Урок 2: Создание игрового экрана и основной логики

## 📋 Что мы будем создавать в этом уроке

Теперь, когда у нас есть главное меню, пора создать **самую важную часть игры** — игровой экран! Мы создадим:
1. **Игровой экран** с кругом букв и полем ввода
2. **Систему генерации уровней** — чтобы каждый уровень был уникальным
3. **Базовую игровую логику** — проверка слов, подсчет очков
4. **Управление состоянием игры** — сохранение прогресса

---

## 🎯 Цель урока
Создать полноценный игровой экран, где игрок может:
- Видеть буквы, расположенные по кругу
- Набирать слова из этих букв
- Видеть загаданные слова
- Получать обратную связь о своих действиях

---

## 📁 Шаг 1: Создание структуры игрового экрана

### 1.1 Обновляем HTML (index.html)
Найдем блок `#gameScreen` в файле `index.html` и заменим его на:

```html
<!-- Игровой экран -->
<div id="gameScreen" class="screen">
    <!-- ХЕДЕР (верхняя панель) -->
    <div class="game-header">
        <!-- Кнопка "Назад" -->
        <button id="backBtn" class="back-btn">
            <i class="fas fa-arrow-left"></i>
        </button>
        
        <!-- Центральная часть с информацией -->
        <div class="header-center">
            <!-- Уровень -->
            <div class="header-stat level-display">
                <i class="fas fa-trophy"></i>
                <span id="currentLevel">1</span>
            </div>
            
            <!-- Очки -->
            <div class="header-stat score-display">
                <i class="fas fa-star"></i>
                <span id="score">0</span>
            </div>
            
            <!-- Попытки -->
            <div class="header-stat attempts-display">
                <i class="fas fa-fire"></i>
                <span id="remainingAttempts">0</span>/<span id="totalLetters">0</span>
            </div>
        </div>
        
        <!-- Правая часть с кнопками действий -->
        <div class="header-right">
            <button id="hintBtnHeader" class="hint-btn-header" title="Открыть слово">
                <i class="fas fa-lightbulb"></i>
            </button>
            <button id="revealBtnHeader" class="reveal-btn-header" title="Открыть букву">
                <i class="fas fa-eye"></i>
            </button>
        </div>
    </div>
    
    <!-- ОБЛАСТЬ СЛОВ (представление загаданных слов) -->
    <div class="words-area">
        <div class="words-grid" id="wordsGrid">
            <!-- Сюда JavaScript добавит строки с буквами -->
        </div>
        <div class="word-info" id="wordInfo">
            Найди все слова из доступных букв
        </div>
    </div>
    
    <!-- ОБЛАСТЬ ВВОДА (круг с буквами) -->
    <div class="input-area">
        <!-- Текущий ввод (буквы, которые игрок набрал) -->
        <div id="currentInput" class="current-input">
            <!-- Сюда JavaScript добавит набранные буквы -->
        </div>
        
        <!-- Круг с буквами -->
        <div class="circle-container" id="circleInput">
            <!-- Сюда JavaScript добавит круг с буквами -->
        </div>
        
        <!-- Кнопки управления -->
        <div class="input-controls">
            <button id="clearBtn" class="control-btn clear-btn" title="Очистить">
                <i class="fas fa-backspace"></i>
            </button>
            <button id="resetBtn" class="control-btn reset-btn" title="Сбросить набранные буквы">
                <i class="fas fa-undo"></i>
            </button>
            <button id="shuffleBtn" class="control-btn shuffle-btn" title="Перемешать буквы">
                <i class="fas fa-random"></i>
            </button>
        </div>
    </div>
</div>
```

**Объяснение структуры:**
1. **`game-header`** — верхняя панель с информацией и кнопками
2. **`words-area`** — область, где отображаются загаданные слова (пока скрытые)
3. **`input-area`** — основная игровая область с кругом букв
4. **`circle-container`** — контейнер для круга (буквы появятся через JS)
5. **`input-controls`** — кнопки управления вводом

---

### 🧪 Тест 1: Проверяем структуру игрового экрана
Обновите временный код в конце `index.html`:

```html
<script>
// Тестируем переключение на игровой экран
document.getElementById('infiniteMode').addEventListener('click', () => {
    console.log('🔄 Переключаемся на игровой экран');
    showScreen('gameScreen');
    
    // Тестируем наполнение игрового экрана
    setTimeout(() => {
        // Заполняем текущий ввод тестовыми буквами
        const currentInput = document.getElementById('currentInput');
        currentInput.innerHTML = `
            <div class="input-letter">К</div>
            <div class="input-letter">О</div>
            <div class="input-letter">Т</div>
        `;
        
        // Заполняем сетку слов тестовыми словами
        const wordsGrid = document.getElementById('wordsGrid');
        wordsGrid.innerHTML = `
            <div class="word-row">
                <div class="word-cell">
                    <div class="letter-cell">К</div>
                    <div class="letter-cell">О</div>
                    <div class="letter-cell">Т</div>
                </div>
            </div>
            <div class="word-row">
                <div class="word-cell">
                    <div class="letter-cell revealed">Д</div>
                    <div class="letter-cell">?</div>
                    <div class="letter-cell">?</div>
                </div>
            </div>
        `;
        
        // Обновляем информацию
        document.getElementById('wordInfo').textContent = 'Найдено слов: 1/2';
        document.getElementById('currentLevel').textContent = '1';
        document.getElementById('score').textContent = '150';
        document.getElementById('remainingAttempts').textContent = '3';
        document.getElementById('totalLetters').textContent = '5';
        
    }, 100);
});
</script>
```

**Что должно получиться:**
1. Нажмите "Бесконечная игра" — перейдете на игровой экран
2. Увидите тестовые буквы в области ввода
3. Увидите тестовые слова (частично открытые)
4. Увидите заполненную статистику в хедере

---

## 🎨 Шаг 2: Стили для игрового экрана

### 2.1 Добавляем стили хедера в components.css
Добавим в конец файла `css/components.css`:

```css
/* 
  ХЕДЕР ИГРОВОГО ЭКРАНА
  Верхняя панель с информацией
*/
.game-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    padding: 6px 10px;               /* Маленькие отступы */
    flex-shrink: 0;                  /* Не сжимается */
    height: 50px;                    /* Фиксированная высота */
    background: rgba(255, 255, 255, 0.05); /* Полупрозрачный фон */
    border-radius: 10px;             /* Скругленные углы */
    gap: 8px;                        /* Расстояние между элементами */
}

/* 
  КНОПКА "НАЗАД"
*/
.back-btn {
    width: 34px;                     /* Квадратная кнопка */
    height: 34px;
    border-radius: 8px;              /* Скругленные углы */
    background: rgba(255, 255, 255, 0.08); /* Полупрозрачный фон */
    border: none;                    /* Без рамки */
    color: #f8fafc;                  /* Белый цвет иконки */
    font-size: 0.9rem;               /* Размер иконки */
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;                 /* Курсор-указатель */
    flex-shrink: 0;                  /* Не сжимается */
}

/* 
  ЦЕНТРАЛЬНАЯ ЧАСТЬ ХЕДЕРА
*/
.header-center {
    display: flex;
    align-items: center;
    gap: 12px;                       /* Расстояние между элементами */
    flex: 1;                         /* Занимает всё доступное пространство */
    justify-content: center;         /* Центрируем элементы */
}

/* 
  ЭЛЕМЕНТ СТАТИСТИКИ В ХЕДЕРЕ
*/
.header-stat {
    display: flex;
    align-items: center;
    gap: 5px;                        /* Расстояние между иконкой и текстом */
    font-size: 0.75rem;              /* Мелкий текст */
    font-weight: 600;                /* Жирный шрифт */
    padding: 4px 8px;                /* Внутренние отступы */
    border-radius: 6px;              /* Скругленные углы */
    min-width: 60px;                 /* Минимальная ширина */
    justify-content: center;         /* Центрируем содержимое */
}

/* 
  СТИЛИ ДЛЯ УРОВНЯ
*/
.level-display {
    background: rgba(59, 130, 246, 0.1); /* Полупрозрачный синий */
    color: #60a5fa;                  /* Синий текст */
}

/* 
  СТИЛИ ДЛЯ ОЧКОВ
*/
.score-display {
    background: rgba(245, 158, 11, 0.1); /* Полупрозрачный оранжевый */
    color: #f59e0b;                  /* Оранжевый текст */
}

/* 
  СТИЛИ ДЛЯ ПОПЫТОК
*/
.attempts-display {
    background: rgba(255, 255, 255, 0.05); /* Очень прозрачный белый */
    color: #94a3b8;                  /* Серый текст */
}

/* 
  ПРАВАЯ ЧАСТЬ ХЕДЕРА
*/
.header-right {
    display: flex;
    align-items: center;
    gap: 6px;                        /* Расстояние между кнопками */
    flex-shrink: 0;                  /* Не сжимается */
}

/* 
  КНОПКА ПОДСКАЗКИ В ХЕДЕРЕ
*/
.hint-btn-header {
    width: 34px;                     /* Квадратная кнопка */
    height: 34px;
    border-radius: 8px;              /* Скругленные углы */
    background: rgba(245, 158, 11, 0.2); /* Полупрозрачный оранжевый */
    border: none;                    /* Без рамки */
    color: #f59e0b;                  /* Оранжевый цвет иконки */
    font-size: 0.8rem;               /* Размер иконки */
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;                 /* Курсор-указатель */
    flex-shrink: 0;                  /* Не сжимается */
}

/* 
  КНОПКА ОТКРЫТИЯ БУКВЫ В ХЕДЕРЕ
*/
.reveal-btn-header {
    width: 34px;                     /* Квадратная кнопка */
    height: 34px;
    border-radius: 8px;              /* Скругленные углы */
    background: rgba(34, 197, 94, 0.2); /* Полупрозрачный зеленый */
    border: none;                    /* Без рамки */
    color: #4ade80;                  /* Зеленый цвет иконки */
    font-size: 0.8rem;               /* Размер иконки */
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;                 /* Курсор-указатель */
    flex-shrink: 0;                  /* Не сжимается */
}
```

### 2.2 Добавляем стили для области слов
Продолжаем в `components.css`:

```css
/* 
  ОБЛАСТЬ СЛОВ (где отображаются загаданные слова)
*/
.words-area {
    flex: 0 0 auto;                  /* Не растягивается и не сжимается */
    background: rgba(255, 255, 255, 0.05); /* Полупрозрачный фон */
    border-radius: 12px;             /* Скругленные углы */
    padding: 8px;                    /* Внутренние отступы */
    overflow-y: auto;                /* Вертикальная прокрутка если нужно */
    display: flex;
    flex-direction: column;          /* Элементы в колонку */
    gap: 6px;                        /* Расстояние между элементами */
    height: 200px;                   /* Фиксированная высота */
    margin-bottom: 4px;              /* Отступ снизу */
}

/* 
  СЕТКА СЛОВ (контейнер для всех слов)
*/
.words-grid {
    display: flex;
    flex-direction: column;          /* Слова в колонку */
    gap: 8px;                        /* Расстояние между словами */
    flex: 1;                         /* Занимает всё доступное пространство */
}

/* 
  СТРОКА СЛОВА (один загаданный слово)
*/
.word-row {
    display: flex;
    gap: 4px;                        /* Расстояние между ячейками букв */
    justify-content: center;         /* Центрируем по горизонтали */
    flex-wrap: wrap;                 /* Перенос на новую строку если нужно */
}

/* 
  ЯЧЕЙКА СЛОВА (контейнер для букв одного слова)
*/
.word-cell {
    display: flex;
    gap: 2px;                        /* Очень маленькое расстояние между буквами */
    padding: 4px;                    /* Внутренние отступы */
    background: rgba(255, 255, 255, 0.08); /* Полупрозрачный фон */
    border-radius: 6px;              /* Скругленные углы */
    min-width: 36px;                 /* Минимальная ширина */
    justify-content: center;         /* Центрируем буквы */
}

/* 
  ЯЧЕЙКА БУКВЫ (одна буква в слове)
*/
.letter-cell {
    width: 20px;                     /* Фиксированная ширина */
    height: 20px;                    /* Фиксированная высота */
    background: rgba(255, 255, 255, 0.1); /* Полупрозрачный белый фон */
    border: 1px solid rgba(255, 255, 255, 0.15); /* Тонкая белая рамка */
    border-radius: 3px;              /* Слегка скругленные углы */
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 0.8rem;               /* Размер буквы */
    font-weight: 700;                /* Жирная буква */
    color: #94a3b8;                  /* Серый цвет (если буква не открыта) */
    transition: all 0.3s;            /* Плавный переход для анимации */
}

/* 
  ОТКРЫТАЯ БУКВА (когда игрок угадал или использовал подсказку)
*/
.letter-cell.revealed {
    background: rgba(59, 130, 246, 0.2); /* Полупрозрачный синий фон */
    border-color: rgba(59, 130, 246, 0.5); /* Синяя рамка */
    color: #60a5fa;                  /* Синий текст */
}

/* 
  ИНФОРМАЦИЯ О СЛОВАХ (текст под сеткой)
*/
.word-info {
    text-align: center;              /* Центрируем текст */
    font-size: 0.65rem;              /* Очень мелкий текст */
    color: #94a3b8;                  /* Серый цвет */
    padding: 3px;                    /* Маленькие отступы */
    margin-top: auto;                /* Прижимаем к низу контейнера */
}
```

### 2.3 Добавляем стили для области ввода и круга букв
Продолжаем добавлять в `components.css`:

```css
/* 
  ОБЛАСТЬ ВВОДА (нижняя часть экрана)
*/
.input-area {
    flex: 1;                         /* Занимает всё оставшееся пространство */
    background: rgba(255, 255, 255, 0.05); /* Полупрозрачный фон */
    border-radius: 12px;             /* Скругленные углы */
    padding: 12px;                   /* Внутренние отступы */
    display: flex;
    flex-direction: column;          /* Элементы в колонку */
    gap: 8px;                        /* Расстояние между элементами */
    min-height: 0;                   /* Важно для flexbox */
    position: relative;
}

/* 
  ТЕКУЩИЙ ВВОД (где отображаются набранные буквы)
*/
.current-input {
    display: flex;
    justify-content: center;         /* Центрируем буквы */
    gap: 5px;                        /* Расстояние между буквами */
    min-height: 32px;                /* Минимальная высота */
    flex-wrap: wrap;                 /* Перенос на новую строку если много букв */
    flex-shrink: 0;                  /* Не сжимается */
    margin-bottom: 5px;              /* Отступ снизу */
}

/* 
  НАБРАННАЯ БУКВА (одна буква в текущем вводе)
*/
.input-letter {
    width: 30px;                     /* Фиксированная ширина */
    height: 30px;                    /* Фиксированная высота */
    background: rgba(59, 130, 246, 0.2); /* Полупрозрачный синий фон */
    border: 2px solid rgba(59, 130, 246, 0.5); /* Синяя рамка */
    border-radius: 5px;              /* Скругленные углы */
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1rem;                 /* Размер буквы */
    font-weight: 700;                /* Жирная буква */
    color: #60a5fa;                  /* Синий цвет */
    animation: popIn 0.2s;           /* Анимация появления */
}

/* 
  КОНТЕЙНЕР ДЛЯ КРУГА
*/
.circle-container {
    position: relative;              /* Позиционирование для букв */
    width: 220px;                    /* Фиксированная ширина */
    height: 220px;                   /* Фиксированная высота */
    margin: 0 auto 15px auto;        /* Центрируем и добавляем отступ снизу */
    flex-shrink: 0;                  /* Не сжимается */
}

/* 
  ЦЕНТРАЛЬНАЯ КНОПКА ПОДТВЕРЖДЕНИЯ
*/
.circle-center {
    position: absolute;              /* Абсолютное позиционирование */
    top: 50%;                        /* Середина по вертикали */
    left: 50%;                       /* Середина по горизонтали */
    transform: translate(-50%, -50%); /* Центрируем точно по середине */
    width: 55px;                     /* Диаметр круга */
    height: 55px;
    background: linear-gradient(135deg, #3b82f6, #1d4ed8); /* Синий градиент */
    border-radius: 50%;              /* Делаем круг */
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.2rem;               /* Размер иконки */
    color: white;                    /* Белая иконка */
    cursor: pointer;                 /* Курсор-указатель */
    box-shadow: 0 3px 10px rgba(59, 130, 246, 0.3); /* Тень */
    z-index: 10;                     /* Поверх букв */
}

/* 
  ЭФФЕКТ ПРИ НАЖАТИИ НА ЦЕНТРАЛЬНУЮ КНОПКУ
*/
.circle-center:active {
    transform: translate(-50%, -50%) scale(0.95); /* Уменьшаем при нажатии */
}

/* 
  БУКВА В КРУГЕ (одна из 10 букв вокруг центра)
*/
.circle-letter {
    position: absolute;              /* Абсолютное позиционирование */
    width: 36px;                     /* Диаметр круга для буквы */
    height: 36px;
    background: rgba(255, 255, 255, 0.1); /* Полупрозрачный белый фон */
    border: 2px solid rgba(255, 255, 255, 0.2); /* Тонкая белая рамка */
    border-radius: 50%;              /* Делаем круг */
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.1rem;               /* Размер буквы */
    font-weight: 700;                /* Жирная буква */
    color: white;                    /* Белый текст */
    cursor: pointer;                 /* Курсор-указатель */
    transition: all 0.2s;            /* Плавный переход */
    z-index: 5;                      /* Ниже центральной кнопки */
}

/* 
  ЭФФЕКТ ПРИ НАЖАТИИ НА БУКВУ
*/
.circle-letter:active {
    background: rgba(59, 130, 246, 0.3); /* Делаем синее при нажатии */
    transform: scale(0.95);          /* Немного уменьшаем */
}

/* 
  КНОПКИ УПРАВЛЕНИЯ ПОД КРУГОМ
*/
.input-controls {
    display: flex;
    justify-content: center;         /* Центрируем кнопки */
    gap: 8px;                        /* Расстояние между кнопками */
    margin-top: auto;                /* Прижимаем к низу контейнера */
    padding-top: 10px;               /* Отступ сверху */
    border-top: 1px solid rgba(255, 255, 255, 0.1); /* Разделительная линия */
    flex-shrink: 0;                  /* Не сжимается */
}

/* 
  ОБЩИЕ СТИЛИ ДЛЯ КНОПОК УПРАВЛЕНИЯ
*/
.control-btn {
    width: 38px;                     /* Квадратные кнопки */
    height: 38px;
    border-radius: 8px;              /* Скругленные углы */
    border: none;                    /* Без рамки */
    font-size: 0.9rem;               /* Размер иконки */
    cursor: pointer;                 /* Курсор-указатель */
    display: flex;
    align-items: center;
    justify-content: center;
    transition: all 0.2s;            /* Плавный переход */
}

/* 
  ЭФФЕКТ ПРИ НАЖАТИИ НА КНОПКИ УПРАВЛЕНИЯ
*/
.control-btn:active {
    transform: scale(0.95);          /* Уменьшаем при нажатии */
}

/* 
  КНОПКА "ОЧИСТИТЬ" (красная)
*/
.clear-btn {
    background: rgba(239, 68, 68, 0.2); /* Полупрозрачный красный */
    color: #f87171;                  /* Светло-красная иконка */
}

/* 
  КНОПКА "ПЕРЕМЕШАТЬ" (фиолетовая)
*/
.shuffle-btn {
    background: rgba(139, 92, 246, 0.2); /* Полупрозрачный фиолетовый */
    color: #a78bfa;                  /* Светло-фиолетовая иконка */
}

/* 
  КНОПКА "СБРОСИТЬ" (оранжевая)
*/
.reset-btn {
    background: rgba(245, 158, 11, 0.2); /* Полупрозрачный оранжевый */
    color: #f59e0b;                  /* Оранжевая иконка */
}
```

---

### 🧪 Тест 2: Проверяем стили игрового экрана
Обновите тестовый код в конце `index.html`:

```html
<script>
// Добавляем тестирование круга букв
function createTestCircle() {
    const circleContainer = document.getElementById('circleInput');
    
    // Центральная кнопка
    const center = document.createElement('div');
    center.className = 'circle-center';
    center.innerHTML = '<i class="fas fa-check"></i>';
    circleContainer.appendChild(center);
    
    // Буквы по кругу
    const letters = ['К', 'О', 'Т', 'Д', 'М', 'А', 'Р', 'Л', 'С', 'Н'];
    const radius = 75; // Радиус круга
    const centerX = 110; // Центр по X
    const centerY = 110; // Центр по Y
    
    letters.forEach((letter, index) => {
        // Вычисляем позицию буквы по кругу
        const angle = (index / letters.length) * 2 * Math.PI; // Угол в радианах
        const x = centerX + radius * Math.cos(angle); // X координата
        const y = centerY + radius * Math.sin(angle); // Y координата
        
        const btn = document.createElement('div');
        btn.className = 'circle-letter';
        btn.textContent = letter;
        btn.style.left = `${x - 18}px`; // 18 = половина ширины буквы
        btn.style.top = `${y - 18}px`;  // 18 = половина высоты буквы
        
        // Добавляем обработчик клика
        btn.addEventListener('click', () => {
            console.log(`🎯 Нажата буква: ${letter}`);
            // Здесь будет логика добавления буквы
        });
        
        circleContainer.appendChild(btn);
    });
}

// Тестируем переключение на игровой экран
document.getElementById('infiniteMode').addEventListener('click', () => {
    console.log('🔄 Переключаемся на игровой экран');
    showScreen('gameScreen');
    
    // Заполняем тестовыми данными
    setTimeout(() => {
        // Очищаем предыдущие тестовые данные
        document.getElementById('circleInput').innerHTML = '';
        
        // Создаем тестовый круг
        createTestCircle();
        
        // Тестируем клик по центральной кнопке
        document.querySelector('.circle-center').addEventListener('click', () => {
            console.log('✅ Нажата центральная кнопка проверки');
        });
        
        // Тестируем кнопки управления
        document.getElementById('clearBtn').addEventListener('click', () => {
            console.log('🗑️ Нажата кнопка "Очистить"');
        });
        
        document.getElementById('shuffleBtn').addEventListener('click', () => {
            console.log('🔀 Нажата кнопка "Перемешать"');
            // Просто меняем цвет букв для демонстрации
            document.querySelectorAll('.circle-letter').forEach(letter => {
                letter.style.background = 'rgba(139, 92, 246, 0.3)';
                setTimeout(() => {
                    letter.style.background = '';
                }, 200);
            });
        });
        
    }, 100);
});
</script>
```

**Что должно получиться:**
1. Нажмите "Бесконечная игра" — увидите полноценный игровой экран
2. Увидите круг с 10 буквами вокруг центральной кнопки
3. Попробуйте кликнуть на буквы — в консоли будут сообщения
4. Нажмите на центральную кнопку — увидите сообщение
5. Нажмите "Перемешать" — буквы на мгновение поменяют цвет

---

## 📝 Шаг 3: Создаем список слов для игры

### 3.1 Создаем файл data/word-list.js
Этот файл будет хранить все слова для игры:

```javascript
// data/word-list.js
// =================
// В этом файле храним ВСЕ данные со словами для игры

// Общий список слов для бесконечного режима
const WORD_LIST = {
    // Основной список слов (простые слова для начала)
    common: [
        'КОТ', 'ДОМ', 'МАК', 'РОТ', 'ЛЕС', 'СОН', 'НОС', 'РАК', 'КОЗА', 'ЛИСА',
        'МОРЕ', 'РЕКА', 'ПОЛЕ', 'ГОРА', 'РУКА', 'НОГА', 'ГЛАЗ', 'УХО', 'РОТ', 'НОС',
        'ВОДА', 'ОГОНЬ', 'ВЕТЕР', 'СОЛНЦЕ', 'ЛУНА', 'ЗВЕЗДА', 'ПТИЦА', 'РЫБА', 'ЦВЕТОК', 'ДЕРЕВО',
        'СТОЛ', 'СТУЛ', 'ДВЕРЬ', 'ОКНО', 'КНИГА', 'РУЧКА', 'БУМАГА', 'ЧАСЫ', 'ТЕЛЕФОН', 'КОМПЬЮТЕР'
    ],
    
    // Тематические списки для ежедневной игры
    dailyThemes: [
        { 
            name: 'Животные', 
            words: ['КОТ', 'СОБАКА', 'ЛЕВ', 'СЛОН', 'ТИГР'],
            letters: ['К', 'О', 'Т', 'С', 'Б', 'А', 'Л', 'Е', 'В', 'Н', 'И', 'Г', 'Р']
        },
        { 
            name: 'Города', 
            words: ['МОСКВА', 'ПАРИЖ', 'ЛОНДОН', 'ТОКИО', 'БЕРЛИН'],
            letters: ['М', 'О', 'С', 'К', 'В', 'А', 'П', 'Р', 'И', 'Ж', 'Л', 'Н', 'Д', 'Т', 'Б', 'Е']
        },
        { 
            name: 'Профессии', 
            words: ['ВРАЧ', 'УЧИТЕЛЬ', 'ПОВАР', 'ИНЖЕНЕР', 'ПРОГРАММИСТ'],
            letters: ['В', 'Р', 'А', 'Ч', 'У', 'И', 'Т', 'Е', 'Л', 'Ь', 'П', 'О', 'Н', 'Ж', 'Г', 'М', 'С']
        },
        { 
            name: 'Еда', 
            words: ['ЯБЛОКО', 'ХЛЕБ', 'СУП', 'САЛАТ', 'ТОРТ'],
            letters: ['Я', 'Б', 'Л', 'О', 'К', 'Х', 'Е', 'С', 'У', 'П', 'А', 'Т', 'Р']
        }
    ],
    
    // Русский алфавит (может пригодиться)
    alphabet: 'АБВГДЕЁЖЗИЙКЛМНОПРСТУФХЦЧШЩЪЫЬЭЮЯ'
};

// Делаем переменную доступной для других файлов
// В JavaScript это называется "глобальная переменная"
window.WORD_LIST = WORD_LIST;
```

**Объяснение:**
1. **`common`** — обычные слова для бесконечного режима
2. **`dailyThemes`** — тематические наборы для "Слова дня"
3. **`alphabet`** — весь русский алфавит (может понадобиться позже)
4. **`window.WORD_LIST`** — делаем переменную глобальной, чтобы другие файлы могли её использовать

---

### 🧪 Тест 3: Проверяем загрузку списка слов
Добавьте в тестовый код в конце `index.html`:

```html
<script>
// Проверяем, что список слов загрузился
console.log('📚 Список слов загружен:', WORD_LIST);
console.log('📖 Первые 5 слов:', WORD_LIST.common.slice(0, 5));
console.log('🎭 Первая тема:', WORD_LIST.dailyThemes[0]);

// Тестируем выбор случайного слова
function getRandomWord() {
    const words = WORD_LIST.common;
    const randomIndex = Math.floor(Math.random() * words.length);
    return words[randomIndex];
}

console.log('🎲 Случайное слово:', getRandomWord());
</script>
```

**Что должно быть в консоли:**
1. Весь список слов
2. Первые 5 слов
3. Первую тему ("Животные")
4. Случайное слово из списка

---

## 🔧 Шаг 4: Создаем утилиты (вспомогательные функции)

### 4.1 Создаем файл js/utils.js
Этот файл будет содержать общие функции, которые используются в разных местах:

```javascript
// js/utils.js
// ============
// В этом файле храним вспомогательные функции, которые используются в разных местах

const Utils = {
    // 1. ФУНКЦИЯ ДЛЯ ПЕРЕМЕШИВАНИЯ МАССИВА
    // Нужно для перемешивания букв в круге
    shuffleArray(array) {
        // Создаем копию массива, чтобы не менять оригинал
        const newArray = [...array];
        
        // Алгоритм Фишера-Йетса (самый эффективный для перемешивания)
        for (let i = newArray.length - 1; i > 0; i--) {
            // Выбираем случайный индекс от 0 до i
            const j = Math.floor(Math.random() * (i + 1));
            
            // Меняем местами элементы i и j
            // Это называется "деструктурирующее присваивание"
            [newArray[i], newArray[j]] = [newArray[j], newArray[i]];
        }
        
        return newArray;
    },
    
    // 2. ФУНКЦИЯ ДЛЯ ПОЛУЧЕНИЯ УНИКАЛЬНЫХ ЭЛЕМЕНТОВ
    // Нужно для получения уникальных букв из слов
    getUnique(array) {
        // Set хранит только уникальные значения
        // ... оператор "spread" преобразует Set обратно в массив
        return [...new Set(array)];
    },
    
    // 3. ФУНКЦИЯ ДЛЯ ПОДСЧЕТА ПОВТОРЯЮЩИХСЯ ЭЛЕМЕНТОВ
    // Нужно чтобы знать, сколько раз каждая буква встречается
    countOccurrences(array) {
        const counts = {}; // Объект для хранения счетчиков
        
        array.forEach(item => {
            // Если буква уже встречалась, увеличиваем счетчик
            // Если не встречалась, инициализируем с 1
            counts[item] = (counts[item] || 0) + 1;
        });
        
        return counts;
    },
    
    // 4. ФУНКЦИЯ ПРОВЕРКИ, МОЖНО ЛИ СОСТАВИТЬ СЛОВО
    // Проверяет, есть ли все нужные буквы в доступных
    canFormWord(word, availableLetters, availableCounts) {
        // Разбиваем слово на буквы
        const wordLetters = word.split('');
        
        // Считаем, сколько раз каждая буква нужна
        const wordCounts = this.countOccurrences(wordLetters);
        
        // Проверяем каждую букву
        for (const letter in wordCounts) {
            // Если буквы нет в доступных или её недостаточно
            if (!availableCounts[letter] || availableCounts[letter] < wordCounts[letter]) {
                return false; // Не можем составить слово
            }
        }
        
        return true; // Все буквы есть в нужном количестве
    },
    
    // 5. ФУНКЦИЯ ДЛЯ ПОЛУЧЕНИЯ СЕГОДНЯШНЕЙ ДАТЫ
    // Нужно для "Слова дня"
    getTodayString() {
        return new Date().toDateString();
    },
    
    // 6. ФУНКЦИЯ ДЛЯ ПОЛУЧЕНИЯ ИНДЕКСА ТЕМЫ ДНЯ
    getDailyThemeIndex() {
        // Получаем день месяца (1-31)
        const dayOfMonth = new Date().getDate();
        // Используем остаток от деления, чтобы индекс был от 0 до 3
        return dayOfMonth % WORD_LIST.dailyThemes.length;
    }
};

// Делаем утилиты доступными глобально
window.Utils = Utils;
```

**Объяснение функций:**
1. **`shuffleArray`** — перемешивает массив (для букв в круге)
2. **`getUnique`** — убирает дубликаты из массива
3. **`countOccurrences`** — считает, сколько раз встречается каждый элемент
4. **`canFormWord`** — проверяет, можно ли составить слово из доступных букв
5. **`getTodayString`** — получает сегодняшнюю дату в виде строки
6. **`getDailyThemeIndex`** — вычисляет, какую тему показывать сегодня

---

### 🧪 Тест 4: Проверяем утилиты
Добавьте в тестовый код:

```html
<script>
// Тестируем утилиты
console.log('🔧 Тестируем утилиты:');

// 1. Перемешивание массива
const testArray = [1, 2, 3, 4, 5];
console.log('🔀 Перемешанный массив:', Utils.shuffleArray(testArray));
console.log('📌 Оригинальный массив не изменился:', testArray);

// 2. Уникальные элементы
const arrayWithDuplicates = ['А', 'Б', 'А', 'В', 'Б', 'Г'];
console.log('🎯 Уникальные элементы:', Utils.getUnique(arrayWithDuplicates));

// 3. Подсчет повторений
console.log('🔢 Подсчет повторений:', Utils.countOccurrences(arrayWithDuplicates));

// 4. Проверка составления слова
const availableLetters = ['К', 'О', 'Т', 'Д', 'О', 'М'];
const availableCounts = Utils.countOccurrences(availableLetters);
console.log('📝 Можно составить "КОТ"?', Utils.canFormWord('КОТ', availableLetters, availableCounts));
console.log('📝 Можно составить "ДОМ"?', Utils.canFormWord('ДОМ', availableLetters, availableCounts));
console.log('📝 Можно составить "МАК"?', Utils.canFormWord('МАК', availableLetters, availableCounts));

// 5. Дата
console.log('📅 Сегодняшняя дата:', Utils.getTodayString());
console.log('🎭 Индекс темы дня:', Utils.getDailyThemeIndex());
</script>
```

---

## 🎮 Шаг 5: Создаем систему состояний игры

### 5.1 Создаем файл js/game-state.js
Этот файл будет хранить текущее состояние игры:

```javascript
// js/game-state.js
// =================
// В этом файле храним ВСЁ состояние игры (уровень, очки, найденные слова и т.д.)

class GameState {
    constructor() {
        // ИНИЦИАЛИЗАЦИЯ СОСТОЯНИЯ ПО УМОЛЧАНИЮ
        this.mode = 'infinite';          // Режим игры: 'infinite' или 'daily'
        this.level = 1;                  // Текущий уровень
        this.score = 0;                  // Количество очков
        this.hints = 3;                  // Количество подсказок
        this.revealAttempts = 5;         // Количество попыток открыть букву
        
        // ТЕКУЩАЯ СЕССИЯ
        this.currentInput = [];          // Буквы, которые игрок набрал сейчас
        this.foundWords = [];            // Слова, которые игрок уже нашел
        this.attemptsUsed = 0;           // Использованные попытки в этом уровне
        this.totalLettersCount = 0;      // Общее количество букв в уровне
        
        // ЕЖЕДНЕВНАЯ ИГРА
        this.dailyCompleted = false;     // Пройдено ли сегодняшнее слово
        this.dailyDate = null;           // Дата последней ежедневной игры
        
        // ДАННЫЕ ТЕКУЩЕГО УРОВНЯ
        this.levelData = null;           // Здесь будем хранить данные уровня
    }
    
    // МЕТОД ДЛЯ ЗАГРУЗКИ ИЗ СОХРАНЕНИЯ
    initFromSave(savedData) {
        if (savedData && savedData.state) {
            // Object.assign копирует свойства из savedData.state в this
            Object.assign(this, savedData.state);
        }
    }
    
    // МЕТОД ДЛЯ СБРОСА СОСТОЯНИЯ ПЕРЕД НОВОЙ ИГРОЙ
    resetForNewGame(mode) {
        this.mode = mode;
        this.currentInput = [];
        this.foundWords = [];
        this.attemptsUsed = 0;
        this.totalLettersCount = 0;
        this.levelData = null;
    }
    
    // МЕТОД ДЛЯ ОБНОВЛЕНИЯ ПОСЛЕ ПРОХОЖДЕНИЯ УРОВНЯ
    updateAfterLevelComplete() {
        if (this.mode === 'infinite') {
            // В бесконечном режиме повышаем уровень
            this.level++;
            this.hints++;
            this.revealAttempts += 2;
        } else {
            // В ежедневном режиме отмечаем как пройденное
            this.dailyCompleted = true;
        }
    }
    
    // МЕТОД ДЛЯ ПОЛУЧЕНИЯ ДАННЫХ ДЛЯ СОХРАНЕНИЯ
    getSaveData() {
        return {
            state: {
                mode: this.mode,
                level: this.level,
                score: this.score,
                hints: this.hints,
                revealAttempts: this.revealAttempts,
                dailyCompleted: this.dailyCompleted,
                dailyDate: this.dailyDate
            }
        };
    }
}

// Экспортируем класс для использования в других файлах
window.GameState = GameState;
```

**Объяснение класса GameState:**
1. **Конструктор** — задает начальные значения
2. **`initFromSave`** — загружает сохраненную игру
3. **`resetForNewGame`** — готовит состояние для новой игры
4. **`updateAfterLevelComplete`** — обновляет после победы
5. **`getSaveData`** — готовит данные для сохранения

---

### 🧪 Тест 5: Проверяем систему состояний
Добавьте в тестовый код:

```html
<script>
// Тестируем систему состояний
console.log('🎮 Тестируем GameState:');

// Создаем экземпляр состояния
const gameState = new GameState();
console.log('📊 Начальное состояние:', gameState);

// Тестируем сброс для новой игры
gameState.resetForNewGame('infinite');
console.log('🔄 Состояние после сброса:', gameState);

// Тестируем обновление после прохождения уровня
gameState.updateAfterLevelComplete();
console.log('🏆 Состояние после победы:', gameState);

// Тестируем получение данных для сохранения
const saveData = gameState.getSaveData();
console.log('💾 Данные для сохранения:', saveData);
</script>
```

---

## 📊 Шаг 6: Создаем менеджер экранов

### 6.1 Создаем файл js/screen-manager.js
Этот файл будет управлять переключением между экранами:

```javascript
// js/screen-manager.js
// ====================
// В этом файле управляем переключением между экранами игры

const ScreenManager = {
    // МЕТОД ДЛЯ ПОКАЗА ЭКРАНА
    show(screenId) {
        console.log(`🔄 Переключаем экран на: ${screenId}`);
        
        // 1. Скрываем ВСЕ экраны
        document.querySelectorAll('.screen').forEach(screen => {
            screen.classList.remove('active');
        });
        
        // 2. Показываем НУЖНЫЙ экран
        const targetScreen = document.getElementById(screenId);
        if (targetScreen) {
            targetScreen.classList.add('active');
        } else {
            console.error(`❌ Экран с id "${screenId}" не найден!`);
        }
    },
    
    // МЕТОД ДЛЯ ПОЛУЧЕНИЯ ТЕКУЩЕГО ЭКРАНА
    getCurrentScreen() {
        const screens = document.querySelectorAll('.screen');
        for (const screen of screens) {
            if (screen.classList.contains('active')) {
                return screen.id;
            }
        }
        return null; // Если ни один экран не активен
    },
    
    // МЕТОД ДЛЯ ПРОВЕРКИ, АКТИВЕН ЛИ ЭКРАН
    isScreenActive(screenId) {
        const screen = document.getElementById(screenId);
        return screen ? screen.classList.contains('active') : false;
    }
};

// Делаем доступным глобально
window.ScreenManager = ScreenManager;
```

### 6.2 Обновляем тестовый код для использования ScreenManager
Замените функцию `showScreen` в тестовом коде:

```html
<script>
// Удаляем старую функцию showScreen и используем ScreenManager
console.log('🎬 Тестируем ScreenManager:');

// Тестируем переключение экранов
document.getElementById('infiniteMode').addEventListener('click', () => {
    ScreenManager.show('gameScreen');
    console.log('📱 Текущий экран:', ScreenManager.getCurrentScreen());
});

document.getElementById('dailyMode').addEventListener('click', () => {
    ScreenManager.show('resultScreen');
    console.log('📱 Текущий экран:', ScreenManager.getCurrentScreen());
});

// Проверяем, какой экран активен сейчас
console.log('🔍 Главный экран активен?', ScreenManager.isScreenActive('mainScreen'));
console.log('🔍 Игровой экран активен?', ScreenManager.isScreenActive('gameScreen'));
</script>
```

---

## 📝 Краткий итог второго урока

### ✅ Что мы создали:
1. **Полноценный игровой экран** с кругом букв, областью слов и управлением
2. **Список слов для игры** — данные для генерации уровней
3. **Утилиты** — общие функции для работы с массивами и данными
4. **Систему состояний игры** — хранение прогресса игрока
5. **Менеджер экранов** — профессиональное переключение между экранами

### 🎨 Что мы узнали:
- **Как создавать сложные интерфейсы** с помощью HTML и CSS
- **Как работать с позиционированием** (absolute, relative) для создания круга
- **Как организовывать данные** в структурированные объекты
- **Как создавать классы в JavaScript** для объектно-ориентированного программирования
- **Как создавать модульную архитектуру** с разделением ответственности

### 🛠️ Ключевые технологии:
```javascript
// Классы в JavaScript
class GameState {
    constructor() { /* инициализация */ }
    method() { /* метод */ }
}

// Работа с DOM
document.querySelectorAll('.screen')
element.classList.add('active')
element.classList.remove('active')

// Манипуляции с массивами
[...new Set(array)] // уникальные элементы
[arr[i], arr[j]] = [arr[j], arr[i]] // обмен значениями
```

### 🔄 Как работает система экранов теперь:
```javascript
// Профессиональная версия
ScreenManager.show('gameScreen'); // Показать экран
ScreenManager.getCurrentScreen(); // Получить текущий экран
ScreenManager.isScreenActive('mainScreen'); // Проверить активность
```

---

## 🏠 Домашнее задание

### Задание 1: Улучшите круг букв
1. Добавьте анимацию при клике на букву
2. Сделайте так, чтобы при наведении буква немного увеличивалась
3. Добавьте звуковой эффект при клике (используйте HTML5 Audio)

### Задание 2: Расширьте список слов
1. Добавьте новые темы для ежедневной игры
2. Создайте систему сложности (простые/средние/сложные слова)
3. Добавьте категории слов (природа, техника, наука и т.д.)

### Задание 3: Улучшите интерфейс
1. Добавьте прогресс-бар для попыток
2. Сделайте анимацию при наборе правильного слова
3. Добавьте подсветку доступных букв после набора слова

### Задание 4: Эксперименты с логикой
1. Напишите функцию, которая проверяет, является ли слово существующим
2. Создайте систему подсказок с разными уровнями (открыть букву/слово/всё)
3. Реализуйте систему комбо (дополнительные очки за несколько слов подряд)

---

## ⏭️ Что будет в третьем уроке

**В третьем уроке мы:**
1. Создадим **генератор уровней** 🎲
2. Реализуем **логику проверки слов** ✅
3. Добавим **систему очков и бонусов** ⭐
4. Создадим **экран результатов** 🏆
5. Реализуем **сохранение прогресса** 💾

**Подготовьтесь:** 
- Придумайте 10-15 новых слов для игры
- Подумайте, какие бонусы могут быть в игре
- Поэкспериментируйте с разными алгоритмами генерации уровней

---

**💡 Важный совет для новичков:**
Не бойтесь экспериментировать! Если что-то не работает:
1. Проверьте консоль браузера (F12 → Console)
2. Используйте `console.log()` для отладки
3. Разбивайте сложные задачи на маленькие шаги
4. Комментируйте код, чтобы не забыть, что делает каждая часть

**🎉 Отличная работа!** Вы создали полноценный игровой экран. В следующем уроке мы добавим самую интересную часть — игровую логику!

**❓ Вопрос для размышления:** Как вы думаете, почему мы разделили код на столько файлов? Что было бы, если бы весь код был в одном файле?
